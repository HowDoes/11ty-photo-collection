---
eleventyExcludeFromCollections: true
---

<ul>
{% for post in collections.photo %}
  <li>
  <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>

  {% image post.data.image, post.data.imagealt, undefined, post.page %}
  
  </li>
{% endfor %}
</ul>
